{
  "name": "freelanceflow",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "dev": "echo 'Use client/ and server/ separately'"
  }
}